//! Spread operator is used to copy content from one array or object into another one

let shoppingList = ["eggs", "milk", "butter"];
console.log(shoppingList);

let shoppingBasket = [...shoppingList, "spinach", "apple", "salmon"];
console.log(shoppingBasket);
